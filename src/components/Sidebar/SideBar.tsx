import React from 'react'
import "styled-components/macro"
import { Color } from '../../variables'

const SideBar = () => {
    return (
        <div css={`background-color: ${Color.Cinder};`}>
          This will be the sidebar
        </div>
    )
}

export default SideBar