import React from "react";
import styled from "styled-components/macro";

const Pass = styled.div`
  text-align: center;
  padding 20px;
  width: 100%;
  border: 1px solid #e5eafa;
`;

const Password = () => {
  return <Pass>Password Settings</Pass>;
};

export default Password;
