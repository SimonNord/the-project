import React from "react";
import styled from "styled-components";

const Settings = styled.div`
  text-align: center;
`;

const ProfileAndSettings = () => {
  return <Settings>Profile And Settings</Settings>;
};

export default ProfileAndSettings;
